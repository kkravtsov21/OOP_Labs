package com.example.Lab6;




public class Tape extends Camera {
    protected int zoom;
    public Tape (String name, int model,int zoom) {
        super(name,model);
        this.zoom=zoom;
    }
}
